"use strict";
/*import { Block } from "./blocks/block"
import {DB} from "./dynamo"
export = class Workflow {
    private idUser : string;
    private workflowName : string;
    private blocks : {
        welcomText : string;
        block: any [];
    };
    
    constructor(idU: string, wf: string) {
        this.idUser = idU;
        this.workflowName = wf;
        let work: Promise <string> = DB.getWorkflow(idU, wf);
        this.blocks.welcomText = work.Items[0].WelcomeText;
        this.blocks.block = work.Items[0].Blocks;
    }
    
    function runBlocks () {
        
    }
}*/ 
